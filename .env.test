# ========== 测试环境配置 ==========
#
# 此文件用于自动化测试，不应包含真实的 API 凭据！
#
# 用途：
# - 运行协议测试: python test_mcp_server.py
# - CI/CD 自动化测试
# - 快速验证 HTTP 传输功能
#
# 使用方法：
# 1. 复制到 .env: cp .env.test .env
# 2. 运行协议测试（无需 API keys）: python test_mcp_server.py
# 3. 如需功能测试，在 .env 中添加真实 API keys: python test_mcp_server.py --with-api
#
# 注意：可以安全提交到版本控制（不含敏感信息）
# ========================================

# ========== HTTP 传输配置 ==========
MCP_TRANSPORT=http
MCP_HOST=127.0.0.1
MCP_PORT=8000

# ========== 测试用认证令牌 ==========
# 这是测试用的 token，不是生产环境的真实凭据
MCP_AUTH_TOKEN=test-token

# ========== 会话管理 ==========
MCP_ENABLE_SESSIONS=true
MCP_SESSION_TIMEOUT=3600

# ========== SSE 配置 ==========
MCP_ENABLE_SSE=true

# ========== 日志配置 ==========
# 测试时使用详细日志便于调试
MCP_LOG_LEVEL=INFO
MCP_DEBUG=true

# ========== 测试图像保存目录 ==========
# 使用独立的测试目录，不污染生产数据
MCP_IMAGE_SAVE_DIR=./test_generated_images
# MCP_PUBLIC_BASE_URL=http://127.0.0.1:8000
MCP_IMAGE_RECORD_TTL=86400
MCP_GET_IMAGE_DATA_MAX_BYTES=10485760

# ========== API 凭据 ==========
# ⚠️ 不要在此文件中填写真实的 API keys！
# ⚠️ API keys 应该填写在你的本地 .env 文件中（已被 .gitignore 排除）
#
# 协议测试（protocol tests）不需要 API keys
# 功能测试（functional tests）需要在 .env 中配置至少一个 API key
#
# 如需功能测试，在 .env 文件中添加（不要在这里添加）：
# TENCENT_SECRET_ID=你的真实密钥
# TENCENT_SECRET_KEY=你的真实密钥
# OPENAI_API_KEY=你的真实密钥
# DOUBAO_API_KEY=你的真实密钥（Ark API）
